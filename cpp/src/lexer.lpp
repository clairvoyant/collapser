%{
    #include <stdio.h>
    #include "common.h"
    int linenumber;
    extern int yylval;
%}
%%
\n                         { linenumber++; }
[0-9]+                     { yylval = atoi(yytext); return NUM; }
\"[^\"\n]*\"               { return STR; }
[a-zA-Z][a-zA-Z0-9]*       { return ID; }
\)                         { return RPAREN;   }
\(                         { return LPAREN; }
.
%%
